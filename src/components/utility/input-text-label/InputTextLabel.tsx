import React from 'react';
import * as CSS from './styles.ts';

const InputTextLabel = ({text}: {text: string}) => {
  return <CSS.InputTextLabel>{text}</CSS.InputTextLabel>;
};

export default InputTextLabel;
